spring:
  datasource:
    url:
    username:
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  messages:
    basename: message
    encoding: UTF-8
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
#    com.example.swcamp_p03.copyGroup.service: trace
